# KG Eval

A lightweight toolkit for generating and evaluating knowledge graphs from text datasets.

## Features

- Load datasets (Hugging Face / CSV / Parquet)
- Extract question text column
- Build simple knowledge graphs (NetworkX-based)
- Compute structural graph metrics
- Export analysis reports (JSON)

---

## Installation

```bash
pip install -e .